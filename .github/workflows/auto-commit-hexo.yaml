name: commit HexoBlogSource (Obsidian update)

on:
  push:
    branches:
      - main

jobs:
  submit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: git config
        run: |
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          if [[ -n $(git status --porcelain) ]]; then
              git commit -m "Obsidian Update"
          fi

      - name: New Branch to pull
        run: |
          git subtree split --prefix=博客文章\博客文章_post -b Hexo_post
          git checkout Hexo_post
          git add .
          if [[ -n $(git status --porcelain) ]]; then
              git commit -m "Obsidian Update"
          fi
          
          

      - name: Push to target repository
        uses: ad-m/github-push-action@v0.5.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          directory: /source/__post/
          repository: WhaleFell/HexoBlogSource
